<%- include ../partials/head -%>

<title>FAQ</title>

<%- include ../partials/header -%>

<h1>FAQ</h1>

<p>
  This page is deticated to all the FAQ questions your website contains. Every questions has it's own answer attached to it.<br>
  Add a question by pressing the 'Add new question' button, or edit one by selecting it's title.
</p>

<a href="/subterra/faq/add" class="button">Add new question</a>

<% if (system.faq.length > 0) { %>
  <ul data-list="faq">
    <% for (let i = 0; i < system.faq.length; i++) { %>
      <li>
        <a href="/subterra/faq/edit/<%= system.faq[i].id %>">
          <%= system.faq[i].question + '?' %>
        </a>
        <button data-action="delete type" onclick="utils.removeItem('faq', <%= system.faq[i].id %>)">delete</button>
      </li>
    <% } %>
  </ul>
<% } else { %>
  <p>No FAQ questions found.</p>
<% } %>

<%- include ../partials/footer -%>
